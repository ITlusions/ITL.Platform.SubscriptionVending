apiVersion: v1
kind: Service
metadata:
  name: subscription-vending
  labels:
    app: subscription-vending
spec:
  selector:
    app: subscription-vending
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
